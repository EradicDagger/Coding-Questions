HashMap<Integer,Boolean> m =  new HashMap<>();
    for (int j = 0; j < a.length); j++)
         {
            if (m.get(a[j]) == null) 
                System.out.print(a[j] + " "); 
                 mp.put(a[j], true); 
        } 
    } 
